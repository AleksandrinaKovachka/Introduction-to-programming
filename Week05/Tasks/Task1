#include <iostream>

using namespace std;

int main()
{
  int k, n;
	bool flag = false;

	cin >> k;
	cin >> n;

	for (int i = 0; i <= n; ++i)
	{
		if (i == k)
		{
			flag = true;
			break;
		}
	}

	flag ? cout << "Yes\n" : cout << "No";
  return 0;
}
